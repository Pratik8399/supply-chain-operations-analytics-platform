# Dashboards

`exports/` — pipeline-generated, BI-ready CSVs (regenerated every run).
Connect Tableau / Power BI to this folder; refresh picks up new runs.

Build spec: ../docs/dashboard_wireframe.md (5 pages)
User enablement + training plan: ../docs/dashboard_adoption_guide.md

After building the workbook, save screenshots to `screenshots/` and link
the Executive Overview + Anomaly Center images in the root README.
